import { useCallback } from "react";
export function $$i0(e, t) {
  return useCallback(() => !(e.manifest.parameters && !1 !== e.manifest.parameterOnly) && !t, [e.manifest.parameters, e.manifest.parameterOnly, t]);
}
export const t = $$i0;