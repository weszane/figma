import { createContext } from "react";
import { lQ } from "../905/934246";
let a = {
  tonePosition: [0, 0],
  canResetTone: !1,
  setTonePosition: lQ,
  setPositionAndRun: lQ
};
let $$s0 = createContext(a);
export const u = $$s0;