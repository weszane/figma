/**
 * Enum representing different types of components
 * Original code: $$n1
 */
export enum SimpleComponentType {
  COMPONENT = "component",
  PLUGIN = "plugin",
  WIDGET = "widget",
}

/**
 * Enum representing different list views
 * Original code: $$i0
 */
export enum OrgListViewType {
  MAIN = 0,
  ORG_PRIVATE_EXPANDED_LIST = 1,
  ORG_ALLOWLIST_EXPANDED_LIST = 2,
}

// Export the enums with the original export names for compatibility
export const s = SimpleComponentType
export const Y = OrgListViewType
