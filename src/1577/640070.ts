import { createContext } from "react";
import { lQ } from "../905/934246";
export let $$o0 = createContext({
  onClick: () => !1,
  toggleQuickReply: lQ,
  onMouseMove: lQ,
  onRemove: lQ,
  inDesktopTray: !1
});
export const H = $$o0;