import set from "../vendor/776892"; // Original: i - likely _.set or object assignment utility
import forOwn from "../vendor/716509"; // Original: s - likely _.forOwn
import createIteratee from "../vendor/162537"; // Original: o - likely _.iteratee

/**
 * Creates an object with the same keys as object and values generated by running 
 * each own enumerable string keyed property of object thru iteratee.
 * 
 * @param object - The object to iterate over
 * @param iteratee - The function invoked per iteration
 * @returns Returns the new mapped object
 */
function mapValues(object: object, iteratee: Function): object {
  const result = {};
  const normalizedIteratee = createIteratee(iteratee, 3);
  
  forOwn(object, function (value, key, obj) {
    set(result, key, normalizedIteratee(value, key, obj));
  });
  
  return result;
}

module.exports = mapValues;
