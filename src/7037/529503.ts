import { setupLazyComponentFactory } from "../905/992467";
import _require from "../draftjs_composer/364326";
export let $$$$o0 = setupLazyComponentFactory("lazy_feed_comment_composer", {
  isCodesplit: !0,
  ComponentFactory: async () => ({
    default: (await Promise.all([]).then(_require)).FeedPostCommentComposer
  })
});
export const o = $$$$o0;