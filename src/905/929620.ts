import { jsx } from 'react/jsx-runtime'

export function generateShimmerOverlay({
  borderRadiusStyle: e,
}) {
  return jsx('div', {
    className: 'generate_image_animation_overlay--Shimmer--ag23v',
    style: e,
  })
}
export const A = generateShimmerOverlay
