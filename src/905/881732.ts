import { y } from "../905/958284";
import { bIZ } from "../figma_app/6204";
import { m } from "../905/92222";
let $$s0 = "FeedPostDetailsModal";
let $$o1 = new m("DenyInTeamFeed", "Don't show non team feed overlays when the viewing the team feed post details modal", (e, t) => e.uiState.modalShownType !== $$s0 || y(t.id) === bIZ.id);
export const E = $$s0;
export const h = $$o1;