/**
 * AvatarSize enum (original: $$n0)
 * Represents different avatar sizes.
 */
export enum AvatarSize {
  XSMALL = 10,
  SMALL = 14,
  SMALL16 = 16,
  MEDIUM20 = 20,
  MEDIUM = 24,
  MEDIUM28 = 28,
  LARGE = 32,
  LARGE40 = 40,
  XLARGE = 48,
}

/**
 * AvatarType enum (original: $$r1)
 * Represents different avatar display types.
 */
export enum HAvatarType {
  FIRST_CHARACTER = 0,
  HIDDEN = 1,
}

// Refactored exports (original: P, U)
export const P = AvatarSize;
export const U = HAvatarType;
