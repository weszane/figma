/**
 * LogLevel enum (original: $$n0)
 * Represents different levels of logging.
 */
export enum LogLevelStr {
  INFO = 'info',
  ERROR = 'error',
  WARN = 'warn',
  DEBUG = 'debug'
}

/**
 * Priority enum (original: $$r1)
 * Represents different priority levels.
 */
export enum PriorityStr {
  P0 = 'P0',
  P1 = 'P1',
  P2 = 'P2'
}

// Export variables with original names for compatibility
export const $ = LogLevelStr; // original: $
export const b = PriorityStr; // original: b
