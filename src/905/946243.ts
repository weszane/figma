import { createContext } from "react";
import { Y } from "../figma_app/316019";
export let $$a0 = createContext({
  preferredLayout: "list",
  preferredLayoutLocalStorageKey: "preferred-variable-picker-layout",
  variablePickerSelectionAtom: Y
});
export const B = $$a0;