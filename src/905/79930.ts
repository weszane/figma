// Refactored enum-like object to improve readability and type safety
// Original code name: $$$$n0

// Define a TypeScript enum for better type safety and readability
export enum TeamTemplateType {
  HubFile = "HubFile",
  TeamTemplate = "TeamTemplate",
  TeamTemplateLg = "TeamTemplateLg",
}

// Export the enum with the original name 'n' for compatibility
export const n = TeamTemplateType
