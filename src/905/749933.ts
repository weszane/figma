import { createContext } from "react";
export let $$n0 = createContext({
  initialized: !1,
  initStarted: !1,
  initCompleted: !1,
  statsigPromise: null,
  updateUser: () => {},
  userVersion: 0
});
export const B = $$n0;