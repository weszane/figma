import { baseFigSchema, P } from '../905/458896'
import { createKiwiCodec, parseSchema } from '../figma_app/870879'

export let $$a0 = createKiwiCodec(!0, parseSchema(baseFigSchema))
export const w = $$a0
