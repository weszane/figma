import { jsx } from "react/jsx-runtime";
import { B } from "../905/224000";
import { Z } from "../905/920418";
export function $$s0({
  thumbnailUrl: e,
  coverThumbnail: t,
  name: i
}) {
  return jsx(B, {
    thumbnailUrl: e,
    coverThumbnail: t,
    className: Z,
    name: i
  });
}
export const D = $$s0;