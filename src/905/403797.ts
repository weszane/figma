/**
 * Enum-like object defining widget types.
 * Original code: $$n0 (refactored to WidgetTypes for clarity).
 */
export const WidgetTypes = {
  EMBED_WIDGET: 'EMBED_WIDGET',
  LINK_PREVIEW_WIDGET: 'LINK_PREVIEW_WIDGET',
  AI_SUMMARY_WIDGET: 'AI_SUMMARY_WIDGET',
  HTML_WIDGET: 'HTML_WIDGET',
}

/**
 * Alias for WidgetTypes.
 * Original code: k (kept as is to maintain export compatibility).
 */
export const k = WidgetTypes
